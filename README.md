Instructions
==================
* install ruby-2.7.1
* install node.js and yarn
* copy master.key file from email into config folder
* run `bundle`
* run `yarn`
* run `rails server`
* for tests run `rake`